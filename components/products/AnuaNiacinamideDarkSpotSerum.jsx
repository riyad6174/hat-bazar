import React, { useState, useEffect } from 'react';
import ProductPageLayout from '@/components/ProductPageLayout';

const AnuaNiacinamideDarkSpotSerum = ({ product }) => {
  // For now, this uses the generic layout but is ready for custom UI
  return <ProductPageLayout product={product} />;
};

export default AnuaNiacinamideDarkSpotSerum;
